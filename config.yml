title: Basic arithmetic
description: Learn basic arithmetic
template:
  repo: basic-arithmetic-template
  name: basic-arithmetic
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
